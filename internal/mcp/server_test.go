package mcp

import (
	"testing"
)

// TestNewMCPServer tests the creation of a new MCP server
func TestNewMCPServer(t *testing.T) {
	t.Skip("Skipping test until we have a proper mock for transport.Transport")
}

// TestRegisterTools tests that the server registers the expected tools
func TestRegisterTools(t *testing.T) {
	t.Skip("Skipping test until we have a proper mock for transport.Transport")
}
