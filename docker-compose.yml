services:
  # MCP server service
  r-server-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    image: r-server-mcp
    volumes:
      # Mount the rmd directory to allow the MCP server to access R Markdown files
      - ${RMD_DIR:-./rmd}:/app/rmd
    ports:
      - "22011:22011"
